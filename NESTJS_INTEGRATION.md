# Интеграция NestJS бэкенда с Next.js

## Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# URL вашего NestJS бэкенда
API_URL=http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:3001
```

**Важно:**
- `API_URL` - используется для серверных компонентов Next.js
- `NEXT_PUBLIC_API_URL` - используется для клиентских компонентов (доступен в браузере)

### 2. Использование в серверных компонентах

Серверные компоненты рендерятся на сервере, поэтому запросы идут напрямую к бэкенду:

```typescript
// app/users/page.tsx
import { apiGet } from "@/lib/api";

export default async function UsersPage() {
  const users = await apiGet("/api/users");
  return <div>{/* ... */}</div>;
}
```

**Преимущества:**
- ✅ Нет проблем с CORS
- ✅ Данные доступны сразу при рендеринге (SEO-friendly)
- ✅ Безопасность (API ключи не попадают в браузер)

### 3. Использование в клиентских компонентах

Для интерактивных действий (POST, PUT, DELETE) используйте клиентские компоненты:

```typescript
// components/UserForm.tsx
"use client";

import { apiPost } from "@/lib/api";

export function UserForm() {
  const handleSubmit = async () => {
    await apiPost("/api/users", { name: "John", email: "john@example.com" });
  };
  // ...
}
```

### 4. Использование с React Query

Для клиентских компонентов можно комбинировать с React Query:

```typescript
// queries/useUsersQuery.ts
import { useQuery } from "@tanstack/react-query";
import { apiGet } from "@/lib/api";

export function useUsersQuery() {
  return useQuery({
    queryKey: ["users"],
    queryFn: () => apiGet("/api/users"),
  });
}
```

## Примеры API функций

Все функции находятся в `src/lib/api.ts`:

```typescript
// GET запрос
const users = await apiGet("/api/users", { page: 1, limit: 10 });

// POST запрос
const newUser = await apiPost("/api/users", { name: "John", email: "john@example.com" });

// PUT запрос
const updated = await apiPut("/api/users/1", { name: "Jane" });

// PATCH запрос
const patched = await apiPatch("/api/users/1", { email: "jane@example.com" });

// DELETE запрос
await apiDelete("/api/users/1");
```

## NestJS Роутинг

### Базовый пример контроллера

```typescript
// users.controller.ts
import { Controller, Get, Post, Param, Body } from '@nestjs/common';

@Controller('api/users')  // Базовый путь: /api/users
export class UsersController {
  
  // GET /api/users
  @Get()
  findAll() {
    return { users: [] };
  }

  // GET /api/users/:id
  @Get(':id')
  findOne(@Param('id') id: string) {
    return { id };
  }

  // POST /api/users
  @Post()
  create(@Body() createUserDto: any) {
    return { message: 'User created' };
  }
}
```

### Query параметры

```typescript
@Get()
findAll(
  @Query('page') page?: number,
  @Query('limit') limit?: number,
) {
  // GET /api/users?page=1&limit=10
}
```

В Next.js:
```typescript
const data = await apiGet("/api/users", { page: 1, limit: 10 });
```

## Что нового в NestJS

### Последние обновления (2024-2025):

1. **Standalone Applications** - упрощенная структура для микросервисов
2. **Улучшенная производительность** - более быстрый старт и оптимизация DI
3. **Новые декораторы** - `@Header()`, `@HttpCode()`, `@Redirect()`
4. **Улучшенная валидация** - встроенная поддержка class-validator
5. **GraphQL улучшения** - лучшая интеграция с Apollo
6. **WebSocket улучшения** - более стабильная работа с Socket.io

## ⭐ Рекомендации для удаленного бэкенда

**Для удаленного NestJS бэкенда рекомендуется использовать React Query в клиентских компонентах:**

### Почему React Query, а не Server Actions?

- ✅ **Меньше латентность** - запрос идет напрямую от клиента к бэкенду (1 хоп вместо 2)
- ✅ **Автоматическое кэширование** - React Query управляет кэшем автоматически
- ✅ **Оптимистичные обновления** - можно обновить UI до получения ответа
- ✅ **Автоматический refetch** - данные обновляются при фокусе окна
- ✅ **Лучший UX** - интерактивные обновления без перезагрузки страницы

### Когда использовать что:

1. **Серверные компоненты** - для начальной загрузки данных (SSR, SEO)
   ```typescript
   export default async function Page() {
     const data = await apiGet("/api/users");
     return <UsersList initialData={data} />;
   }
   ```

2. **React Query в клиентских компонентах** - для интерактивных действий (⭐ рекомендуется)
   ```typescript
   const createUser = useCreateUser(); // из useUsersMutation
   createUser.mutate({ name, email });
   ```

3. **Server Actions** - только если:
   - Нужно скрыть API ключи/токены
   - Есть CORS проблемы (но лучше настроить CORS в NestJS!)

📖 **Подробное сравнение:** см. `WHEN_TO_USE_WHAT.md`

## Примеры страниц

- `/api-example` - примеры всех способов подключения к API
- Смотрите `src/app/api-example/page.tsx` для деталей

## Troubleshooting

### CORS ошибки

Если получаете CORS ошибки при запросах из клиентских компонентов, настройте CORS в NestJS:

```typescript
// main.ts
app.enableCors({
  origin: 'http://localhost:3000', // URL вашего Next.js приложения
  credentials: true,
});
```

### Запросы не работают

1. Проверьте, что NestJS бэкенд запущен
2. Проверьте URL в `.env.local`
3. Проверьте, что endpoint существует в NestJS
4. Для серверных компонентов используйте `API_URL`
5. Для клиентских компонентов используйте `NEXT_PUBLIC_API_URL`

